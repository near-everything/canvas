/// <reference types="react" />
import PropTypes from 'prop-types';
export interface LoaderProps {
    label?: string;
}
declare const Loader: {
    ({ label }: LoaderProps): JSX.Element;
    propTypes: {
        label: PropTypes.Requireable<string>;
    };
    defaultProps: {
        label: string;
    };
};
export default Loader;
