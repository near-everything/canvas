import React from 'react';
import Loader from './Loader';
export default {
    title: 'Components/Loader',
    component: Loader,
};
const Template = (args) => React.createElement(Loader, { ...args });
export const Default = Template.bind({});
Default.args = {};
