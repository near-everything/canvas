import React, { useState } from 'react';
import Hint from './Hint';
import { HintProvider, noop } from '../../tests/helpers';
export default {
    title: 'Components/Hint',
    component: Hint,
};
const Template = (args) => {
    const [inputNode, setInputNode] = useState(null);
    return (React.createElement(HintProvider, { hintText: "california", inputNode: inputNode },
        React.createElement(Hint, { ...args },
            React.createElement("input", { className: "form-control form-control-lg", onChange: noop, ref: setInputNode, value: "cal" }))));
};
export const Default = Template.bind({});
Default.args = {};
