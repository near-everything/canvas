import React, { ReactNode } from 'react';
export declare const useHint: () => {
    hintRef: React.MutableRefObject<HTMLInputElement | null>;
    hintText: string;
};
export interface HintProps {
    children: ReactNode;
    className?: string;
}
declare const Hint: ({ children, className }: HintProps) => JSX.Element;
export default Hint;
