import React from 'react';
import MenuItem from './MenuItem';
import { defaultContext, TypeaheadContext, } from '../../core/Context';
export default {
    title: 'Components/MenuItem/MenuItem',
    component: MenuItem,
};
const value = {
    ...defaultContext,
    id: 'test-id',
};
const Template = ({ context, props }) => (React.createElement(TypeaheadContext.Provider, { value: { ...value, ...context } },
    React.createElement(MenuItem, { ...props })));
export const Default = Template.bind({});
Default.args = {
    props: {
        children: 'This is a menu item',
        label: 'test label',
        option: '',
        position: 0,
    },
};
