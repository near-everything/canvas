import useRootClose from './useRootClose';
function RootClose({ children, onRootClose, ...props }) {
    const rootRef = useRootClose(onRootClose, props);
    return children(rootRef);
}
export default RootClose;
