interface Props {
    open?: boolean;
    minLength: number;
    showMenu: boolean;
    text: string;
}
export default function isShown({ open, minLength, showMenu, text, }: Props): boolean;
export {};
