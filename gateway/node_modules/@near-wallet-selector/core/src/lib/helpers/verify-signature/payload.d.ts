/// <reference types="node" />
import type { SignMessageParams } from "../../wallet";
export declare class Payload {
    tag: number;
    message: string;
    nonce: Buffer;
    recipient: string;
    callbackUrl?: string;
    constructor(data: SignMessageParams);
}
export declare const payloadSchema: Map<typeof Payload, {
    kind: string;
    fields: ((string | number[])[] | (string | {
        kind: string;
        type: string;
    })[])[];
}>;
