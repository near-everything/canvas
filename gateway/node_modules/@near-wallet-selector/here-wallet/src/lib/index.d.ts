import type { HereProvider, HereStrategy } from "@here-wallet/core";
import type { WalletModuleFactory } from "@near-wallet-selector/core";
import type { HereWallet } from "./types";
import icon from "./icon";
export { icon };
interface Options {
    deprecated?: boolean;
    iconUrl?: string;
    defaultStrategy?: () => HereStrategy;
    defaultProvider?: HereProvider;
}
export declare function setupHereWallet({ deprecated, iconUrl, defaultStrategy, defaultProvider, }?: Options): WalletModuleFactory<HereWallet>;
