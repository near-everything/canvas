import MultiChainWebSocket from '.';
import { Address, ChainId, Hash } from '../types';
declare type UnsubscribeOptions = {
    id: Address | Hash;
    chainId?: ChainId;
    timeout?: number;
};
declare function unsubscribe(this: MultiChainWebSocket, options: UnsubscribeOptions): void;
export default unsubscribe;
