import { Observable } from 'rxjs';
import MultiChainWebSocket from '.';
import { EthereumTransactionData, Subscription } from '../types';
declare function subscribe(this: MultiChainWebSocket, subscription: Subscription): Observable<EthereumTransactionData | null>;
export default subscribe;
