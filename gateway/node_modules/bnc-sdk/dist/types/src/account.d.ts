import { Emitter } from './types';
import SDK from '.';
declare function account(this: SDK, address: string): {
    emitter: Emitter;
    details: {
        address: string;
    };
};
export default account;
