import { HereStrategy } from "../types";
import { HereProviderRequest } from "../provider";
import QRCode, { QRSettings } from "./qrcode";
import logo from "./logo";
export { QRCode, logo };
export interface QRCodeStrategyOptions extends Partial<QRSettings> {
    element: HTMLElement;
    theme?: "dark" | "light";
    animate?: boolean;
    mainnet?: string;
    testnet?: string;
}
export declare class QRCodeStrategy implements HereStrategy {
    options: QRCodeStrategyOptions;
    private qrcode?;
    readonly mainnet: string;
    readonly testnet: string;
    constructor(options: QRCodeStrategyOptions);
    get themeConfig(): QRSettings;
    onRequested(id: string, request: HereProviderRequest): void;
    close(): void;
    onFailed: () => void;
    onSuccess: () => void;
}
export declare const darkQR: QRSettings;
export declare const lightQR: QRSettings;
